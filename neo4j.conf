# Neo4j Configuration for Railway
# Minimal working configuration with correct Neo4j 5.15 settings

# Network
server.default_listen_address=0.0.0.0
server.bolt.listen_address=:7687
server.http.listen_address=:7474

# Memory (adjust based on Railway plan)
server.memory.heap.initial_size=256m
server.memory.heap.max_size=256m
server.memory.pagecache.size=128m

# Security
server.security.auth_enabled=true

# APOC procedures (correct Neo4j 5.x syntax)
dbms.security.procedures.unrestricted=gds.*,apoc.*
dbms.security.procedures.allowlist=gds.*,apoc.*